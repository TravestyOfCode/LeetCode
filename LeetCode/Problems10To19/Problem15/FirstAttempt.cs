using System.Collections.Generic;

namespace LeetCode.Problems10To19.Problem15
{
    public class FirstAttempt
    {
        public IList<IList<int>> ThreeSum(int[] nums)
        {
            // Given an array of ints, return all the triplets (nums[i], nums[j], nums[k])
            // where i != j, i != k and j != k and nums[i] + nums[j] + nums[k] == 0.
            // Do not include solutions more than once.

            return new List<IList<int>>();
        }
    }
}
